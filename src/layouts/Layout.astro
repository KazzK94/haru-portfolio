---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Haru Blue - Digital Artist Portfolio" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-blue-100 to-blue-300">
    <slot />

    <style>
      body {
        background: linear-gradient(
          135deg,
          rgba(15, 9, 107, 1) 0%,
          rgba(33, 33, 168, 1) 35%,
          rgba(0, 212, 255, 1) 100%
        );
		margin: 0;
      }
    </style>

    <script>
      const disclaimerAccepted = localStorage.getItem("disclaimerAccepted");
	  initialDisclaimer();
      function initialDisclaimer() {
		if(disclaimerAccepted) return;
        if (
          confirm(
            "DISCLAIMER:\nEsta página web es un prototipo y se irá mejorando. Se aceptan críticas pero NO VALE QUEJARSE, HIJUEPUTES!!!!\n\n¿Te parece bien?"
          )
        ) {
          localStorage.setItem("disclaimerAccepted", "true");
          alert("¡Gracias por tu comprensión! 😊");
        } else {
          alert("Vete a la mierda, entonces. 😡");
        }
      }
    </script>
  </body>
</html>
